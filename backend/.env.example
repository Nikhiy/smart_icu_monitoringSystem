# Backend environment variables
# Copy this file to backend/.env and adjust values as needed.

# Host and port for the Flask server
BACKEND_HOST=0.0.0.0
BACKEND_PORT=5000

# Model configuration
MODEL_DIR=model
USE_TENSORFLOW=auto

# Fallback model storage path
FALLBACK_MODEL_PATH=model/fallback_model.pkl
